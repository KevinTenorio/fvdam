// Importa o controller e o context (se tiver), cria o componente com nome geral e o exporta para o resto da aplicação

import MeshGenerator from './MeshGenerator.controller';

export default MeshGenerator;
